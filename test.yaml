apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: mar-8-beta-04
  region: us-west-2

iam:
  withOIDC: true

nodeGroups:
  - name: ng-1
    instanceType: t3.medium
    desiredCapacity: 2
    minSize: 1
    maxSize: 3
